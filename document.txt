/* RULES MY PROGRAMING */
    * Перед функциями и переменными (3 чистых строки)

/* Описание функции по порядку описания */
    * Задача функции
    ~ Описание переменных (Если есть)
        ~ В "::" Описываем через знак "/" без пробелов:
            * "param" -  т.к. опсываем пораметр
            * <Названия параметра>
            * <тип пораметра>
        * Далее описываем переменную
        * При нужде в "{}" указываем возможные параметры переменной
          указываем:
            * Значение которое принимает переменная
            * ":" - знак разделения
            * Описание данного значения
            * ",\n" - если предпологаются ещё аргументы
    ~ Описываем возвращяемые параметры (Если есть)
        ~ В "::" Описываем через знак "/" без пробелов:
            * "returne" - т.к. описываем возвращяемую переменную
            * <тип переменной>
        * Далее описываем переменную

